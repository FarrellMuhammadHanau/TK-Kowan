apiVersion: apps/v1
kind: Deployment
metadata:
  name: attendance-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: attendance-postgres
  template:
    metadata:
      labels:
        app: attendance-postgres
    spec:
      containers:
      - name: postgres
        image: postgres:15
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_DB
          value: attendance_service_db
        - name: POSTGRES_USER
          value: postgres
        - name: POSTGRES_PASSWORD
          value: postgres
        volumeMounts:
        - name: postgres-storage
          mountPath: /var/lib/postgresql/data
          subPath: postgres
      volumes:
      - name: postgres-storage
        persistentVolumeClaim:
          claimName: attendance-postgres-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: attendance-postgres
spec:
  selector:
    app: attendance-postgres
  ports:
  - port: 5432
    targetPort: 5432
  type: ClusterIP
