apiVersion: apps/v1
kind: Deployment
metadata:
  name: attendee-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: attendee-postgres
  template:
    metadata:
      labels:
        app: attendee-postgres
    spec:
      containers:
      - name: postgres
        image: postgres:15
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_DB
          value: attendee_service_db
        - name: POSTGRES_USER
          value: postgres
        - name: POSTGRES_PASSWORD
          value: postgres
        volumeMounts:
        - name: postgres-storage
          mountPath: /var/lib/postgresql/data
          subPath: postgres
      volumes:
      - name: postgres-storage
        persistentVolumeClaim:
          claimName: attendee-postgres-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: attendee-postgres
spec:
  selector:
    app: attendee-postgres
  ports:
  - port: 5432
    targetPort: 5432
  type: ClusterIP