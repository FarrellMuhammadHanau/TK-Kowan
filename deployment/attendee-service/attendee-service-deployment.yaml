apiVersion: apps/v1
kind: Deployment
metadata:
  name: attendee-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: attendee-service
  template:
    metadata:
      labels:
        app: attendee-service
    spec:
      containers:
      - name: attendee-service
        image: farrellmuhammadhanau/attendee-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
        env:
        - name: DATABASE_URL
          value: postgresql+asyncpg://postgres:postgres@attendee-postgres:5432/attendee_service_db
        - name: JWT_SECRET
          value: EfEmEitch123
---
apiVersion: v1
kind: Service
metadata:
  name: attendee-service
spec:
  selector:
    app: attendee-service
  ports:
  - port: 8000
    targetPort: 8000
  type: ClusterIP